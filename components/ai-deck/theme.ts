/**
 * The deck's one accent colour — Knead's brand red, as declared in globals.css
 * (`--accent-knead`). It lives in its own module so the slide content, the
 * shell and the demo panels can all reach it without importing each other in a
 * cycle.
 */
export const ACCENT = '#FF6B6B';
